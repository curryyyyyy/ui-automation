# 数据服务平台门户

本目录存放门户自身的页面对象与 P0 链路。当前已确认门户地址为 `/dashboard`，门户 P0-PORTAL-00 验证 SSO 登录后可进入首页。

门户页面元素只能写在 `locators/`，页面操作和断言只能写在 `pages/`。门户以“卡片标题 + 同卡片中的立即使用按钮”进入二级平台，不依赖同名按钮的页面顺序。不要把机器标注或资源管理的业务元素、菜单或断言写入本目录。
