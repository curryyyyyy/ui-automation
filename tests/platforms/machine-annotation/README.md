# 机器标注平台

本目录独占机器标注平台的页面对象、元素定位、数据工厂和 P0 用例。`locators/` 只声明页面元素，`pages/` 只编排操作与断言。

已确认入口路径为 `/hawk/project`，页面就绪态为“新建项目”按钮可见。`P0-ANNOTATION-01` 验证用户从门户卡片进入项目管理页。后续创建项目、批次检索和模板操作必须继续在本目录内维护。

业务 P0 使用共享 SSO Fixture 的 `authenticatedPage`，不得重复填写账号密码。新增创建项目等写入用例时，必须注入 `dataScope`，并在项目创建后立即登记平台内删除清理。
